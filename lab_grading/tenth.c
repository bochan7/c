#include <stdio.h>
int main() {
    int count = 0;
    /* Print 20 asterisks using a while loop*/
    while (count < 20) {
        printf("*");
        count++;
    }
    printf("\n");
    return 0;
}
